pipeline:  
  something:
    image: busybox
    commands: 
      - ls -alRt /root
    volumes:
      - /secrets:/root

  docker:
    image: plugins/docker
    repo: cloudsc/delete-me
    secrets: [ docker_username, docker_password ]
